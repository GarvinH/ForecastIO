export enum weatherMode {
    forecast,
    current
}

export enum searchMode {
    city,
    coord
}

export enum actionTypes {
    changeWeatherMode,
    changeSearchMode
}

export enum measurementSys {
    Kelvin,
    Celcius,
    Fahrenheit
}